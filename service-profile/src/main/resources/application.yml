server:
  port: 9201
spring:
  application:
    name: service-profile
  cloud:
    loadbalancer:
      ribbon:
        # disable Spring Cloud Netflix Ribbon to enable Spring Cloud LoadBalancer
        enabled: false

logging:
  level:
    org.springframework: debug
